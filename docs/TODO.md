# TODO — remaining work

> Generated by `trk render` from the in-repo issue tracker (`.tracker/`). Do not edit by hand —
> mutate via `trk add`/`dep`/`in`/`edit`/`state` and regenerate.

> Arc-less drift: 4 remaining task(s) belong to no arc (`trk list --no-arc`).

## Arc-less

- [ ] `01M1FMNSZ` compact GCs a task that NON-TRACKER files cite by id, destroying the provenance of a decision recorded outside the tracker -- four scenarios/registry.tsv rows now cite ids that resolve to nothing #design #compact

  <details><summary>FOUND 2026-09-01 on the Enix tracker, by a lane resolving registry rows…</summary>

  FOUND 2026-09-01 on the Enix tracker, by a lane resolving registry rows held on dead ids.

  THE INSTANCE. `scenarios/registry.tsv` has an `in-par` column whose value 3 REQUIRES a `held` column
  naming the task that owns that row's first verification run. Four rows -- TOOLCONFERTEST,
  TOOLLAUNCHTEST, RING3I2CHID, RING3USBMOUSE -- cite ids (01KYTDVC7, 01KYSWDSN, 01KZV686M) that now
  resolve to nothing.

  THEY ARE NOT TYPOS, and establishing that is what makes this a trk issue rather than an Enix one:
  `git log -S` finds all three cited in the very commits that landed their rows (b0fc533f, d4a5a455,
  78121bef). They were real tasks, honestly cited. `compact` physically GCs archived/dropped tasks, so
  the record is simply gone.

  WHY THAT IS WORSE THAN AN ARCHIVED ID. Seven sibling rows in the same audit were resolved by READING
  the archived owner's body and seeing whether a verification had actually happened -- three were
  cleared on the strength of a recorded PASS, four got fresh owners because their bodies showed a
  feature landing with no boot. For a GC'd id there is no body to read, so "verified, write-back
  forgotten" is indistinguishable from "never booted" and the only honest state is the unverified one
  the rows already carry. A week of gate-governance provenance was destroyed silently.

  THE DESIGN GAP. trk's compaction rules were written about task-to-task references -- `needs` edges,
  arcs, citations inside other task bodies, all of which trk can see. They say nothing about ids cited
  from files OUTSIDE the tracker, which trk cannot see and which are, in this project, load-bearing:
  a registry column, a source comment, a design doc, a commit message.

  SHAPES, none obviously right, which is why this is filed rather than fixed:
    * a pre-compact WARNING listing ids about to be GC'd, so a caller can grep its own tree first. Cheap,
      keeps trk ignorant of its consumers, puts the check where the knowledge is. Probably the answer.
    * an opt-in "cited externally" pin (a tag, or a config-listed glob trk greps before compacting).
      More capable, more coupling.
    * tombstones -- keep id + title + final state forever, GC only the body. Answers "did this exist and
      what was it" without unbounded growth, which is exactly the question a dangling citation asks.
    * rule it a caller responsibility and say so in design.md, so the next person hits documentation
      instead of a mystery.

  RELATED, not the same: `dangling-tracker-id-lint.sh` on the Enix side checks id EXISTENCE and its own
  selftest asserts that a citation of a COMPACTED/ARCHIVED id PASSES -- deliberately, so archival does
  not redden a tree. That is a defensible rule for archived ids and it is why nothing caught these.

  </details>

- [ ] `01M2V2TB9` trk archive has refused for two consecutive sessions and the done queue is 36 deep: the decision-marker guard's false positives are dominated by tasks that quote the markers BECAUSE the markers are their subject -- including the tasks that built the guard #tooling #tracker #scott-decision

  <details><summary>OBSERVED across two consecutive sessions (2026-08-28 and 2026-08-29…</summary>

  OBSERVED across two consecutive sessions (2026-08-28 and 2026-08-29 save-sweeps): `trk archive`
  refuses, the done queue does not graduate, and nothing reaches docs/CHANGELOG.md. The queue is now
  36 tasks deep.

  THE REFUSAL IS WORKING AS DESIGNED and should not be weakened casually -- `archived` is hidden from
  every view, so burying an unresolved fork with a finished task is a real loss, which is exactly why
  the guard exists. The problem is its FALSE POSITIVE RATE on one specific shape.

  `trk archive --dry-run` reports 37 marker hits, and the guard labels most of them "(prose-shaped)".
  Reading them, the dominant class is SELF-REFERENTIAL: tasks whose bodies quote the marker strings
  BECAUSE THE MARKERS ARE THEIR SUBJECT. Concretely, from the dry-run:
    * 01M12D4EV -- four hits, all from its own body explaining which markers the guard matches
      ("The other markers (scott-decision, OPEN QUESTION, FIX NOTE, your call) are ... none of them
      collide with a filename shape the way TODO does").
    * 01M12ZG5E -- six hits, its body literally saying it "necessarily quotes `[TODO]`,
      `[OPEN QUESTION]` and `[scott-decision]` because those markers ARE its subject".
    * 01M135SJX / 01M135SK1 -- hits from ENUMERATING sibling task ids and their tags in a findings
      roll-up ("01M138E21 (HIGH, scott-decision)").
    * 01M1650PW -- the scott-decision tagging pass itself, whose body cannot describe its own job
      without naming the tag, and whose last sentence is "It resolves nothing."
  So the tasks that BUILT AND FIXED the marker guard are the ones the marker guard now refuses to
  archive, and a roll-up task is blocked by naming its children's tags.

  WHY IT COMPOUNDS RATHER THAN SITTING STILL: every session that files a roll-up, an audit summary, or
  any work ABOUT the decision-marker mechanism adds another permanently-stuck entry. The queue only
  grows, and the escape (--allow-buried-decisions) is all-or-nothing across the whole run -- so using it
  to clear five genuine false positives also waves through any REAL buried fork in the other 31, which
  is precisely the loss the guard exists to prevent. That all-or-nothing shape is what has made me
  decline twice: at the end of a long session, auditing 36 bodies to earn one blanket override is not a
  thing to do tired, and doing it untired is still the wrong shape.

  FIX SHAPES (unweighed -- and this is a guard-semantics call, so it is Scott's):
    (a) Per-task escape: --allow-buried-decisions=<id,id,...>, so an operator clears the specific
        false positives they actually read and the guard still protects the rest. Smallest change,
        keeps the guard's teeth, makes the override auditable in the shell history.
    (b) Teach the guard the self-referential shape -- e.g. ignore a marker inside backticks or inside
        a quoted span, which is how every false positive above is written. Riskier: a real fork could
        be quoted too.
    (c) Exempt by tag: a task tagged as tracker/tooling work ABOUT the marker mechanism is expected to
        quote markers. Narrow, but relies on tagging discipline the guard cannot verify.
  (a) is the one I would reach for, because it does not require the guard to get any smarter and it
  leaves a record of exactly which bodies a human cleared.

  RELATED, already filed: 01M13JXWN (a count-PRESERVING body edit defeats archive's per-task count
  assertion) is a different defect in the same command; worth looking at together since both are about
  what archive trusts.


  REC-BOT 2026-08-30 :: OPTIONS: (a) per-task escape `--allow-buried-decisions=<id,...>`; (b) teach the
  guard the self-referential shape (ignore a marker inside backticks or a quoted span); (c) exempt by tag ::
  NEW: (d) the guard ALREADY makes the distinction the fix needs, and nobody has used it. Its own dry-run
  output labels every hit `(prose-shaped)` or `(marker-shaped)`, and I measured the split at HEAD: of 40
  hits, 37 are prose-shaped and 3 are marker-shaped. So narrowing the REFUSAL to marker-shaped hits --
  while still REPORTING the prose-shaped ones -- would unblock the queue today, keep the guard's teeth on
  the class it was built for, and require no new flag, no new tagging discipline, and no quoting heuristic.
  The classifier is already there; only the refusal predicate reads it too broadly ::
  EVIDENCE:
  - `trk archive --dry-run 2>&1 | tail -1` -> "  (40 line(s); a real run without --allow-buried-decisions would refuse)"
  - `trk archive --dry-run 2>&1 | grep -c 'marker-shaped'` -> 3
  - `trk archive --dry-run 2>&1 | grep -c 'prose-shaped'` -> 37
  - dry-run output: "  01M12ZG5E  [scott-decision] (prose-shaped)  necessarily quotes `[TODO]`, `[OPEN QUESTION]` and `[scott-decision]` because those markers ARE its"
  - dry-run output: "  01M1650PW  [scott-decision] (prose-shaped)  Scott, 2026-08-29: the scott-decision tag has been applied inconsistently. ... It resolves nothing."
  - dry-run output: "  01M135SJX  [scott-decision] (prose-shaped)  01M138CEN (HIGH), 01M138DCV (HIGH), 01M138E21 (HIGH, scott-decision), 01M138EGN, 01M138F95, 01M138FMZ."
  - dry-run output: "  01M12ZG5E  [OPEN QUESTION] (marker-shaped)  genuine \"OPEN QUESTION:\" to that same task, archive again -- the old code let it through" -- one of the 3 marker-shaped hits is itself a self-referential quote in a task ABOUT the guard, so even the narrow class is not clean
  - `trk list --state done | wc -l` -> 62 (the body reported 36; the queue has grown, so the compounding claim is measured, not predicted)
  - `trk show 01M13JXWN | sed -n '2p'` -> the sibling defect ("trk archive's per-task count assertion is defeated by a count-PRESERVING body edit"), state open, untagged
  :: WEIGHING: the body's framing is right on every count -- the refusal is working as designed, the escape's
  all-or-nothing shape is what makes it unusable at a session end, and the false-positive class is dominated
  by tasks whose SUBJECT is the markers. The measured split turns that qualitative reading into a number and
  the number is decisive: 37 of 40 are the class the guard should not be refusing on, and the queue has grown
  from 36 to 62 since filing, so the compounding claim is now measured rather than predicted.
  Weighing the four: (a) is the body's own lean and it is genuinely good -- it keeps the teeth, makes the
  override auditable, and requires the operator to have READ each body they clear. Its cost is that it does
  not reduce the reading burden at all; it just makes the burden per-task instead of all-or-nothing, and the
  body's own reason for declining twice was that "auditing 36 bodies to earn one blanket override is not a
  thing to do tired". At 62, per-task clearing is 62 decisions instead of one, which is more auditable and
  more work -- a real improvement in safety, not in usability.
  (b) is the riskiest: a genuine fork CAN be written inside backticks, and the guard exists precisely
  because burying one is unrecoverable (`archived` is hidden from every view). Do not weaken the match.
  (c) relies on tagging discipline the guard cannot verify, which the body says and is right about.
  (d) dominates (a) on usability without giving up what (a) protects, because the split is not a heuristic
  the guard would have to learn -- it is a classification the guard already computes and prints. And it
  degrades safely: a prose-shaped hit is still REPORTED, so an operator reading the dry-run still sees every
  one; only the refusal stops firing on them. The residual risk is the one the dry-run itself shows -- even
  the marker-shaped class contains a self-referential quote (01M12ZG5E's) -- so (d) is not a clean
  separation and should be paired with (a) as the escape for the remainder. Three per-task overrides is a
  tractable session-end action in a way that 62 is not.
  The principle underneath is one this batch keeps hitting: `docs/debugging.md` rule 5's spirit is that a
  check must be able to fail AND that something must act on it. A check that fires so broadly that it is
  never acted on has failed the second half -- and the observed behaviour (two consecutive sessions
  declining, queue growing) is that failure happening. `agent-first.md`'s harness section is the positive
  statement: these tools are load-bearing agent-first surfaces because "an agent parses [their verdicts] by
  name, not prose it eyeballs" -- and the guard's own `(prose-shaped)`/`(marker-shaped)` label is exactly
  such a parseable verdict that nothing currently consumes.
  :: SELF-REFUTE: the strongest fact against (d) is that the prose/marker classifier might be a heuristic
  with its own false NEGATIVES -- if a genuinely buried fork can be written in a form the classifier calls
  prose-shaped, then narrowing the refusal to marker-shaped hits lets exactly the loss the guard exists to
  prevent through the door. I read the dry-run's labels and counted them, but I did NOT read trk's
  classifier implementation to see how it decides between the two (it lives in the sibling trk repo, outside
  this tree). That read is mandatory before (d) ships, and if the classifier is weak, (a) alone is the
  answer.
  :: REC: (d) paired with (a), contingent on reading trk's prose/marker classifier first (see SELF-REFUTE).
  Narrow the REFUSAL to marker-shaped hits while still reporting prose-shaped ones -- that alone drops the
  blocking set from 40 to 3 on today's queue -- and add the per-task `--allow-buried-decisions=<id,...>`
  escape for the remainder, so the three that still block are cleared by an operator who read them, with the
  override visible in shell history. Do NOT take (b): a genuine fork can be written inside backticks, and
  weakening the match trades an unusable guard for an unsafe one. Look at 01M13JXWN in the same pass as the
  body suggests -- both are about what archive trusts, and both are currently untagged/unowned in a way that
  lets the queue grow. High confidence in the ranking; the guard-semantics call itself stays Scott's.
  :: DISPOSITION: proposed


  THE FIX FOR THIS EXISTS BUT IS NOT INSTALLED YET (2026-08-31). A lane landed both trk defects in the
  sibling repo (~/dev/zig/trk commit 45db249, NOT pushed, NOT installed). The live ~/.local/bin/trk is
  still the 2026-08-28 00:20 binary and therefore still has both bugs — verified by mtime, and the lane
  was explicitly fenced off from installing over it because this session and five sibling lanes were
  using it concurrently.

  WHAT CHANGED, and it is a better answer than the one this task was going to get: the per-task escape
  no longer asserts a COUNT. A count is defeated by a count-PRESERVING edit (delete one marker-shaped
  line, append another), and so is any finer sub-count, one shape over. The exemption now asserts the
  hit set's IDENTITY: `--allow-buried-decisions-for <id>:<n>:<digest>`, digest = FNV-1a/32 over the
  matched lines in body order (Cli.hitDigest, src/cli.zig:1930). The count survives only for legibility
  — both must match, so they cannot disagree dangerously, and `15 -> 16` is a diagnosis where a hash
  mismatch is only a verdict. The old `<id>:<n>` form is now a hard error rather than a silent
  downgrade to the weaker guarantee.

  OPERATIONAL RULE FOR WHENEVER THIS QUEUE IS FINALLY DRAINED — read this before pasting any digest.
  finding-20260803005917: "a lane's COMPUTED derived value (hash, pinned id, checksum) is stale on
  arrival if anything changed its derivation"; the rule that predicts it exactly is that a derived value
  survives iff its own input set was untouched by everything that landed meanwhile. A digest here is
  computed over a TASK BODY, and task bodies are exactly what every fan-out lane appends to — this
  session alone has five lanes appending right now, and any one of them can add a marker-shaped line to
  a task in the archive queue. So: NEVER carry a digest across a merge, a wave, or a session. Recompute
  it at the moment of the archive run, from the guard's own printed `<id>:<n>:<digest>` line, which is
  why the lane made `--refuse`/`--dry-run` print the paste-ready form under each unexempted task.
  Treat any digest written down anywhere — including one written above — as a cross-check, never a
  source.

  NOT the finding-20260728182455 trap, checked rather than assumed: that one is about a derived short
  identifier that must be STABLE for life being silently rewritten by compaction. Here invalidation on
  content change is the entire mechanism, so churn is the feature, not the bug. The two look alike and
  are opposites.

  WHAT REMAINS ON THIS TASK: the queue is 62 deep and still blocked, because the tool that would unblock
  it is built but not rolled out. Rollout is orchestrator-owned and deliberately deferred out of a live
  wave. 01M13JXWN and 01M13JXWS stay `claimed` for the same reason — closing them would let `trk archive`
  graduate them into the CHANGELOG while the binary everyone runs still carries both bugs.


  === MIGRATED FROM ENIX TRACKER 2026-09-18 (Scott: "move any other trk-related tasks within enix's tracker over to the trk repo"). Original Enix id: 01M16BBSF, which now holds a pointer to this record. Any pre-migration citation of 01M16BBSF in Enix commits, docs or task bodies refers to THIS task.

  </details>

- [s] `01M2V2TG8` Make a landed ruling remove its own #scott-decision tag: pick the mechanism (a trk rule verb that appends+untags atomically, a lint that fails on a tagged task whose body carries a RULED marker, or splitting the decision from its carrier task at ruling time) #tracker #process #tooling

  <details><summary>Scott 2026-09-11, on being handed an already-answered question: 'it is…</summary>

  Scott 2026-09-11, on being handed an already-answered question: 'it is insane that this is a new task as a scott-decision that asks to remove a scott-decision that should have already been removed... I feel like this is abusing the task system.' Measured the same day: 01M1WWQSC carried three of his rulings in its body (09-07 x2, 09-09) and still read as open; 01M1ZKRWY and 01M1ZM8MM likewise; 11 stale tags came off in one day, and at least 3 had been surfaced to him repeatedly in the interim. Root cause is structural: 'trk edit --append-body' records the ruling and says nothing about the tag. Options, his pick: (1) a 'trk rule <id> <text>' verb that appends the ruling and removes the tag in one step, so the tag cannot be forgotten -- strongest, needs a trk release (sibling repo ../trk); (2) a lint in make unit-fast that FAILS on any open task tagged scott-decision whose body contains a ruling marker (RULED/Scott ruled/RULED BY SCOTT -- note the phrasings differ, so the matcher needs care) -- cheapest, catches it after the fact; (3) split the decision task from its build carrier at ruling time, so the tag leaves with the closed decision. The doctrine itself is already recorded in issue-tracker.md's Settled rulings; this task is the mechanism only.

  SECOND HALF, same complaint: the end-of-turn 'OPEN -- needs you' block re-listed ALL pending decisions every turn (CLAUDE.md mandates the block, not its contents), so Scott saw the same 8-12 items dozens of times in one session and asked 'is this any different from the 20 other times I've been asked about it'. A stale tag made it worse: an answered question kept reappearing. Whatever mechanism is picked here, the reporting rule that goes with it is: surface NEW or CHANGED decisions, and point at the tracker for the standing list. Changing that sentence is a CLAUDE.md edit, which is write-gated -- Scott's call.

  SUBMITTED 2026-09-18: Built (a) — `trk rule <id> <text>` (trk commit e32dd3876, sibling repo ../trk, worktree /home/scott/dev/zig/trk-wt/ruling-untag branch ruling-untag). Appends the ruling to the body (same semantics as `edit --append-body`, `-` reads stdin) and removes the decision tag in the same call. Refuses when the task does not currently carry the tag (structural check on `tags[]`, never on body text) -- the guard against silently closing a genuinely open fork (01M12CKRK's shape). Does NOT close the task; that stays a separate judgment call (`trk state <id> done`).

  WEIGHED ALL THREE: (b) a RULED-marker lint was rejected because a free-text marker convention an author can forget to write is the same failure class as the tag surviving in the first place -- the task's own body already named this weakness ("the phrasings differ, so the matcher needs care"). (c) splitting the decision from its carrier was rejected using tonight's own evidence: 01M29P00C is `state: done` and STILL carries #scott-decision right now, proving closing/archiving a task does not by itself strip the tag -- it only relocates where the same forgettable step would need to happen. (a) is the only one that makes the append+untag atomic in the sense that matters: not a filesystem transaction, but that `rule`'s own code always performs both, so nothing routed through it can land half-ruled -- unlike `edit --append-body`, which has no notion that a given body edit was a ruling.

  HONEST GAP: `rule` still depends on an author reaching for it instead of hand-composing `edit --append-body` + `edit --rm-tag scott-decision`. It cannot make forgetting IMPOSSIBLE -- only smaller (one correctly-named verb vs. remembering two flags together, every time) and structurally incapable of landing half-done once invoked. Recorded plainly in trk's design doc.

  COMPOSES WITH `reportBuriedDecisions` (src/cli.zig, `trk archive`'s buried-decision guard) rather than duplicating it: the guard fires at ARCHIVE time and scans body TEXT for marker words (including the literal string "scott-decision") in tasks about to be hidden forever; `rule` fires while a task is still OPEN and clears a TAG field. A task `rule` untags but that stays open as a carrier never reaches archive under this ruling, so the guard never sees it -- exactly the gap this verb closes, since nothing at archive time can catch a decision stuck open-but-answered.

  CONFIGURABLE: `rule.tag` in `.tracker/config.json` (default `default_decision_tag` = "scott-decision"), mirroring `archive.decision_markers` -- trk itself has no opinion on what a repo calls its tag. Not yet wired into Enix's own config.json (defaults to "scott-decision", byte-identical to today's convention, so no action needed unless a different tag is wanted).

  VERIFIED: `zig build test` in the trk worktree, 279/279 passing. Sabotage-proven both halves (dropped the untag append -> exactly the 3 tag-dependent tests failed with `expected 0, found 1` / tag-still-present, the 4th arg-count test stayed green; restored, restored to green; separately dropped the not-tagged refusal -> exactly the 2 tests exercising it failed with `expected error.UsageError, found error.TestUnexpectedSuccess`; restored, restored to green).

  DOCS: trk's docs/design.md gets a new "Settled rulings" entry (full argument, composition with the buried-decision guard, honest gap, sabotage proof). README.md's command list + a `rule.tag` config example. Enix's docs/design/process/issue-tracker.md's existing "A ruling's landing includes removing the tag" entry updated from "filed as 01M298M9Z" to "Built: trk rule" with a pointer at trk's design doc (Enix commit 2dd2e036f in this worktree). CLAUDE.md NOT touched (write-gated; nothing in this build contradicts it).

  Could not observe: whether Scott wants `rule.tag` actually set in Enix's own .tracker/config.json (currently absent, which resolves to the same default "scott-decision" -- purely cosmetic either way, not filed as a question since the behavior is identical).

  2026-09-18 SAVE SWEEP -- THIS LANE'S WORK IS HELD, NOT ORPHANED. Recorded here because a branch with no owner reads identically to forgotten work, and the tracker is the only place that can tell them apart.

  TWO UNMERGED HANDLES, both deliberate:
    trk repo:  /home/scott/dev/zig/trk-wt/ruling-untag   branch `ruling-untag`   tip e32dd38
    Enix repo: worktree-agent-afcc89380c2b35f96          tip 2dd2e036f (doc-only: issue-tracker.md)

  WHY HELD: a scoped `make test-affected-heal` was compiling when the lane reported, and merging into a tree that a heal is reading makes the heal's output unattributable -- the same race that produced a spurious lint red earlier in this session. Nothing is wrong with the work; the merge simply waits for the heal.

  VERIFIED BEFORE THE HOLD, so a future session need not re-derive it: the lane's refusal is STRUCTURAL -- it iterates `t.tags.items` rather than matching body text, which is the property that makes it un-foolable by prose and unable to silently close a genuinely open fork (01M12CKRK is the paired positive it must never touch). Orchestrator read that in the diff rather than trusting the report. trk `zig build test` 279/279 per the lane, sabotage-proven twice.

  THE MERGE, when the heal lands:
    bash scripts/merge-lane.sh --repo /home/scott/dev/zig/trk --holder agent-afcc89380c2b35f96 /home/scott/dev/zig/trk-wt/ruling-untag e32dd38
    bash scripts/merge-lane.sh .claude/worktrees/agent-afcc89380c2b35f96 2dd2e036f

  AND NOTE, because it will bite: merging trk does NOT put the new `rule` verb into service. The installed binary at ~/.local/bin/trk must be rebuilt (`zig build --prefix ~/.local -Doptimize=ReleaseSafe` in the trk checkout), and the long-running `mcp__trk__*` server keeps executing whatever binary it started with until it restarts. Both were observed this session: the MCP archive dry-run still reported a buried-decision false positive that the freshly-installed CLI had already fixed.


  === MIGRATED FROM ENIX TRACKER 2026-09-18 (Scott: "move any other trk-related tasks within enix's tracker over to the trk repo"). Original Enix id: 01M298M9Z, which now holds a pointer to this record. Any pre-migration citation of 01M298M9Z in Enix commits, docs or task bodies refers to THIS task.

  </details>

- [ ] `01M2V2TSA` trk render/list --arc/next still omit an arc's compacted members — the same silent absence 01M29P5T7 fixed in tree and show #tracker #tooling #scott-decision

  <details><summary>Residual from 01M29P5T7 (trk lane/tree-compacted), filed rather than…</summary>

  Residual from 01M29P5T7 (trk lane/tree-compacted), filed rather than silently scoped out.

  01M29P5T7 made `trk tree` and `trk show` report an arc's GRADUATED (archived+compacted) members instead
  of rendering the arc as empty. The invariant it established: a view that ENUMERATES ARC MEMBERS must
  never present a short-or-empty list as if nothing was ever there, because `compact`'s `gc_set` deletes
  the `in` edge along with the collected member.

  Three views still enumerate arc members and still say nothing about graduated ones:
    - `trk render` -> docs/TODO.md (the projection every reader outside the tool actually reads)
    - `trk list --arc <id>`
    - `trk next` (arc-seq ordering walks the same `ins`)

  Not folded into 01M29P5T7 because docs/TODO.md's format is load-bearing for readers outside trk and the
  right shape there is a judgment call, not an obvious repair: a per-arc "+N compacted" annotation, a
  footer, or nothing at all (TODO.md is by definition forward-looking work, so arguably graduated members
  do not belong in it and only `list`/`next` need it).

  The data is already in the store: `Store.compactedMembers(gpa, arc)` (src/store.zig), landed by
  01M29P5T7, returns every tombstone naming that arc. So this is a display decision plus tests, not a
  recovery problem.

  NOT recoverable, and deliberately so — do not re-file these as bugs: a COMPACTED DEPENDENT (the
  tombstone index records no `needs` edges) and a LIVE task's COMPACTED ARC (the live task has no
  tombstone, and the arc's record says only what IT was a member of). Recovering either means recording
  edges the index intentionally does not carry.

  SCOPE QUESTION FOR SCOTT: does docs/TODO.md want the annotation at all, or only `list`/`next`?

  REC-BOT 2026-09-18 (batch 12)
  OPTIONS: for docs/TODO.md - a per-arc "+N compacted" annotation, a footer, or nothing at all (the body's own reading: TODO.md is by definition forward-looking, so arguably graduated members do not belong in it and only list/next need the fix). The stated scope question: does docs/TODO.md want the annotation at all, or only list/next?
  NEW: RENDER'S OWN CODE ALREADY ANSWERS THE TODO.md HALF, and it answers it NO - not by taste but by a contract the renderer states in its own comment: TODO = only not-yet-built work, show open/blocked, never the finished/abandoned states, because done graduates to the CHANGELOG via archive. A compacted member is archived-then-collected, i.e. the most finished state there is, so annotating it into TODO.md would put the ONE class of task the projection exists to exclude back into it. That is not a display preference, it is a contradiction with the projection's stated domain - which collapses the body's scope question from a judgement call to a reading. The genuinely open remainder is different and smaller than the body frames it: (d) render ALREADY has an empty-arc message, so the silent-absence shape the invariant forbids is present there in a SOFTER form - an arc whose every member graduated renders "no open members under this arc", which is TRUE under the TODO contract and MISLEADING to a reader who knows the arc had members. So the render-side fix, if any, is one word in that existing sentence (open members), not a new annotation - and its correct destination is the CHANGELOG, where those members already are.
  EVIDENCE:
  - /home/scott/dev/zig/trk/src/store.zig:1017 "pub fn compactedMembers(self: *const Store, gpa: std.mem.Allocator, arc: Ulid) ![]const *const Tombstone {"
  - /home/scott/dev/zig/trk/src/cli.zig:3386 "TODO = only not-yet-built work: show open/blocked, never the"
  - /home/scott/dev/zig/trk/src/cli.zig:3409 "no open members under this arc"
  - grep -n 'compactedMembers' /home/scott/dev/zig/trk/src/cli.zig -> 7 call sites, at 3687, 3770, 3791, 3793, 3858 (all inside cmdTree, which begins 3646, and its compactedMembersJson helper) and 4213, 4315 (both inside cmdShow, which begins 4080). None falls inside renderMarkdown (3310-3645), cmdList (3173-3279) or cmdNext (3065-3172) - the body's three-view claim is exact.
  - grep -n 'fn cmdNext|fn cmdList|fn cmdRender|fn renderMarkdown|fn cmdTree|fn cmdShow' /home/scott/dev/zig/trk/src/cli.zig -E -> cmdNext:3065 cmdList:3173 cmdRender:3280 renderMarkdown:3310 cmdTree:3646 cmdShow:4080 (the ranges above)
  WEIGHING: The invariant 01M29P5T7 established is the right one and it is stated in the body correctly - a view that ENUMERATES ARC MEMBERS must never present a short-or-empty list as if nothing was ever there. What decides the fork is that TODO.md is not that kind of view. issue-tracker.md's own model, and CLAUDE.md's restatement of it, make the render a PROJECTION of remaining work whose graduation path is the CHANGELOG; the archive verb exists precisely so a finished member leaves the projection and lands in prose. Under docs-discipline's "cite, don't restate" and the design-doc litmus (would this sentence still earn its place once the work is done?), a "+N compacted" line in TODO.md is a second, drifting copy of what CHANGELOG.md already holds in curated form - two indexes of one fact, which is the accretion the corpus warns against. namespace-identity.md's rule applies one level up too: the count is already a VALUE the store can answer (compactedMembers), so a reader who wants it should ask the tool, not parse it out of a generated markdown file. So: list and next genuinely want it (they are member enumerations with no CHANGELOG twin and their consumer is an agent that will otherwise conclude an arc is empty), and render does not. Against my own reading: agent-first.md would say the human projection should be a faithful render of the structured value, and "this arc has 14 graduated members" IS part of that value - but the same doc's honest brake is that the projection guarantees consistency, not completeness of every field, and the TODO contract is an explicit, ruled narrowing of which fields project.
  SELF-REFUTE: If Scott reads docs/TODO.md as the standing human view of the whole tracker rather than strictly as the remaining-work projection - and the empty-arc case is his actual complaint, i.e. he has hit a bare arc heading and been unable to tell finished from never-filed - then the annotation belongs in TODO.md after all and my reading of the contract comment is a tool author's intent overriding the user's. I did not check this: it is what he means by the projection, which is not evidence-decidable. The existing "no open members under this arc" line is the closest thing to a signal, and it was added for exactly that confusion (01M0ZC286).
  REC: Build it for `list --arc` and `next` only; leave docs/TODO.md's per-arc body unannotated. If the empty-arc case is the real irritant, the minimal honest repair is to make render's existing sentence say what it means - that no member is OPEN, with the count of graduated ones and a pointer to the CHANGELOG - rather than introducing a per-arc annotation the projection's own contract excludes. Add the same both-directions test 01M29P5T7 used: an arc with a graduated member must differ from an arc that never had one, in each of the two views. Confidence: MEDIUM-HIGH on list/next; MEDIUM on the TODO.md no, which the SELF-REFUTE can invert on a single word from Scott.
  DISPOSITION: proposed (stays tagged - the body's own question is explicitly addressed to Scott and turns on what TODO.md is FOR, which is his to say; the code reading narrows the fork but does not settle whose view it is)


  === MIGRATED FROM ENIX TRACKER 2026-09-18 (Scott: "move any other trk-related tasks within enix's tracker over to the trk repo"). Original Enix id: 01M2MWB6Q, which now holds a pointer to this record. Any pre-migration citation of 01M2MWB6Q in Enix commits, docs or task bodies refers to THIS task.

  </details>


